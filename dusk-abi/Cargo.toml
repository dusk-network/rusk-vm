[package]
name = "dusk-abi"
version = "0.1.0"
authors = ["Kristoffer Ström <kristoffer@dusk.network>"]
edition = "2018"

[dependencies]
serde = { version = "*", default-features = false }
fermion = { version = "0.2", default-features = false }
kelvin = { version = "0.11.2", optional = true }
phoenix-abi = { git = "https://github.com/dusk-network/phoenix-abi", default-features = false }

[features]
default = ["std"]
std = ["fermion/std", "kelvin"]
